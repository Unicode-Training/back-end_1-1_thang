## CLI

npx prisma db push --> Đồng bộ schema với database

npx prisma generate --> Tạo ra các hàm tương ứng với schema để sử dụng prisma client

Lưu ý:

Mỗi lần thay đổi file schema (.prisma) => Chạy 2 lệnh trên
